{
  "geode": "5.10.1",
  "gd": {
    "win": "2.2081"
  },
  "id": "holdforge.slc_to_hold",
  "name": "HoldForge",
  "version": "v0.1.4",
  "developer": "HoldForge",
  "description": "Convert Silicate macros into stock-GD native Options Triggers using an automatically recorded in-game trajectory.",
  "tags": [
    "editor",
    "utility"
  ],
  "settings": {
    "conversion": {
      "type": "title",
      "name": "Conversion"
    },
    "strict-240": {
      "type": "bool",
      "name": "Strict 240 TPS",
      "default": true,
      "description": "Reject non-240 TPS macros. Turning this off converts time, but does not reproduce custom-TPS physics."
    },
    "offset-ms": {
      "type": "float",
      "name": "Timing offset (ms)",
      "default": 0,
      "min": -1000,
      "max": 1000,
      "description": "Legacy experimental fallback only. Automatic traces require 0 ms."
    },
    "x-offset": {
      "type": "float",
      "name": "Position offset",
      "default": 0,
      "min": -300,
      "max": 300,
      "description": "Legacy experimental fallback only. Automatic traces require 0; no universal X correction is assumed."
    },
    "trigger-y": {
      "type": "float",
      "name": "Trigger row Y",
      "default": 90,
      "min": -10000,
      "max": 10000
    },
    "editor-layer": {
      "type": "int",
      "name": "Generated editor layer",
      "default": 999,
      "min": 0,
      "max": 999
    },
    "max-triggers": {
      "type": "int",
      "name": "Maximum generated triggers",
      "default": 20000,
      "min": 10,
      "max": 50000
    },
    "backup-level": {
      "type": "bool",
      "name": "Backup before import",
      "default": true,
      "description": "Save the raw editor level string before inserting a batch. Import stops if backup cannot be written."
    },
    "experimental": {
      "type": "title",
      "name": "Experimental"
    },
    "allow-static-fallback": {
      "type": "bool",
      "name": "Allow static timeline fallback",
      "default": false,
      "description": "Experimental only. Allows editor posForTime placement when no valid automatic trace exists. Not publication-safe without a stock-GD hold test."
    },
    "unsafe-timeline": {
      "type": "bool",
      "name": "Experimental timeline",
      "default": false,
      "description": "Allow selected dynamic mechanics only for the experimental editor-time fallback. Reverse/teleport/rotated gameplay remain rejected."
    },
    "allow-existing-controls": {
      "type": "bool",
      "name": "Allow existing control triggers",
      "default": false,
      "description": "Existing Options Triggers may override the generated batch. Normally imports stop when control triggers already exist."
    },
    "debug": {
      "type": "title",
      "name": "Advanced Debug"
    },
    "debug-enabled": {
      "type": "bool",
      "name": "Enable diagnostic journal",
      "default": true,
      "description": "Write a bounded latest.jsonl, flushing each event for crash diagnosis. Export logs creates a single JSON report."
    },
    "debug-inputs": {
      "type": "bool",
      "name": "Log decoded macro inputs",
      "default": false,
      "description": "Frame, input type, player, press/release and TPS. Re-import the macro after enabling."
    },
    "debug-mapping": {
      "type": "bool",
      "name": "Log time-to-position mapping",
      "default": true,
      "description": "Record each generated frame/time/X/P1/P2 and the editor timeline roundtrip."
    },
    "debug-runtime": {
      "type": "bool",
      "name": "Trace playtest inputs and triggers",
      "default": true,
      "description": "Log native Options before/after, actual buttons, P1/P2 state, dual transitions, deaths and playtest state into the one-file export. Logging only; never changes gameplay input."
    },
    "debug-objects": {
      "type": "bool",
      "name": "Include generated object strings",
      "default": false,
      "description": "Include serialization in mapping events. Requires Log time-to-position mapping."
    },
    "debug-mods": {
      "type": "bool",
      "name": "Include mod versions",
      "default": true,
      "description": "Record installed mod IDs, versions and loaded state for compatibility diagnosis. Captured when importing."
    },
    "debug-max-events": {
      "type": "int",
      "name": "Diagnostic event limit",
      "default": 20000,
      "min": 100,
      "max": 100000,
      "description": "Report retains the newest N events. Crash journal writes the first N events of the session. Oldest ring entries are counted as dropped."
    }
  }
}
