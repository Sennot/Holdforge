# Разбор журналов 23 сентября 2026 — HoldForge 0.1.1

Прочитаны previous.jsonl, latest.jsonl и экспорт holdforge-1790184214010928-1790184335291473.json.
Старые JSONL содержат последовательность многострочных JSON, поэтому прочитаны
через последовательный JSON decoder. latest и экспорт описывают одну сессию,
события не складывались дважды.

- Макрос: SLC v3, 240 TPS, 412 действий P1, 0 действий P2, от кадра 228 до 14744.
- Уровень: не 2-player, не стартовый dual; сгенерировано 413 Options.
- В previous последние 412 P1 button событий полностью совпадают с кадрами и
  press/release макроса: во всех случаях progress_tick = 2 * (frame + 1).
  Это наблюдение для этих файлов, не универсальная формула времени GD.
- Все созданные триггеры одновременно переключают P1/P2.
- На начале dual P1 повторяет исходный прогон, а P2 перестаёт повторять его.

| Кадр макроса | P1 Y исходный / hold | P2 Y исходный / hold |
| --- | --- | --- |
| 7271 | 273.21 / 273.21 | 262.66 / 262.66 |
| 7278 | 256.88 / 256.88 | 278.99 / 255.52 |
| 7322 | 225.00 / 225.00 | 327.08 / 206.40 |

Сравнение сделано на соответствующих событиях: Options срабатывает в другой
фазе шага, чем исходный handleButton. В этих строках его progress_tick на 2 меньше,
при этом P1 Y совпадает. Нельзя считать каждый такой сдвиг ошибкой на один кадр.

По всем 412 действиям разность progress_tick (Options - исходный button):
-4: 41 событие; -2: 255; 0: 84; +2: 23; +4: 9.
Есть непостоянное расхождение карты редактора и реального прогона. Постоянное
смещение не исправит весь уровень; значение offset автоматически не менялось.

## Что изменено

Runtime-поправка передаёт физическое удержание P1 в P2 только на время native
обработчика парного Options в обычном dual. Она адресует вероятную причину:
отдельного удержания P2 в single-input dual нет, хотя его управление включается.
После входа в dual восстанавливается общий gate, поскольку новый P2 может
сбросить состояние управления. Независимый режим 2 Player не затрагивается.

Из журналов доказано расхождение траекторий, но внутренняя причина native
обработчика и успешность поправки должны быть подтверждены игровым прогоном.
Восстановление буфера на входе в dual также требует проверки разных режимов.

35 смертей со слов пользователя: прежний журнал их не содержит. Он логировал
editor_damage, но не PlayLayer::destroyPlayer; в присланной сессии editor_damage
и playtest_start отсутствуют. В 0.1.2 добавлены death_attempt/death_result,
attempt_reset, dual_transition, физический ввод, буферы, режимы и level_time.
События смерти могут быть скрыты чужим noclip-хуком; dead_after отличает
переданный запрос уничтожения от смерти, которую увидел наш обработчик.

## Быстрая проверка

Собрать 0.1.2 через Actions. Существующие триггеры можно оставить: Generated
editor layer должен совпадать с их слоем. Включить Shared dual hold compatibility
и Runtime trace, отключить воспроизведение макроса, запустить с начала и держать P1.
Затем Export logs. HoldForge должен оставаться включённым при этом тесте.
Исходный .slc и копия уровня нужны для дальнейшей точной правки таймингов.
